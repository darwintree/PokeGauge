# THROWAWAY: C 对阵条的角色标记

问题：保持已经确认的 64px 精灵图和约 88px 高度，哪种角色标记更符合现有 HUD？只调整顶部宝可梦选择区域。

运行 `pnpm prototype:matchup`，在原页面使用 `?variant=A|B|C`，通过底部浮条或左右方向键切换。

- A 名称铭牌：名称上方放置与能力阶级一致的 ATK / DEF 实色标签。
- B 精灵徽章：将剑盾收进有硬边框的实色徽章，移到精灵右下方，名字居中。
- C 侧边角色条：色条融入卡片左边，名字上方显示紧凑的本地化角色文字。

本轮替换上一轮的 A/B 和裸剑盾 C；前一轮已确认共享外框及 64px 精灵图，角色表现尚未确定。原型仅开发环境显示，对阵修改不自动保存。复用真实宝可梦选择和形态入口。浮条展示身份、方案和高度。

等待用户选择；确定后重新实现正式代码，将完整原型与结论保存到临时分支并留下指针。

已确认：采用 A 名称铭牌，正式实现需恢复 hover 动效，保留键盘焦点与按压反馈。
